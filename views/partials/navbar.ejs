<%if (album && !systemf) {%>
 <div id="upload" class="modal" tabindex="-1" role="dialog">
                            <div class="modal-dialog">
                                <div class="modal-content">
                                    <div class="modal-header">
                                        <button type="button" id="close" class="close" data-dismiss="modal">&times;</button>
                                        <div class="modal-title">Upload Images</div>
                                    </div>

                    <Form enctype="multipart/form-data" role="form" method="post" action="/images-album/upload/<%=album%>"> 
                                    <div class="modal-body">
                                        <div class="row">
                                           <input id="file" name="photo"  type="file" multiple/>
                                       <input type="submit" class="form-control" name="upload" id="upload-input">
                                        </div>
                                    </div>
                                    <form>
                                </div>
                            </div>
                        </div>
<% } %>
<nav class="navbar">
  <div class="container-fluid">
  
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a href="#menu-toggle" class="btn btn-default" id="menu-toggle"><i class="fa fa-bars"></i></a>
    </div>
  
    
  <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
    <form enctype="multipart/form-data" id="upload" action="/upload" method="post" style="display:none">
    <input type="file" name="upload" style="width: 1px; height: 1px" multiple />
    </form>      
      <ul class="nav navbar-nav navbar-right nav_bar">
          <li <%if (systemf) {%>class="nav-item active"<%}%>><a href="/sytem-images?system=true">System</a></li>
          <li <%if (!systemf) {%>class="nav-item active"<%}%>><a href="/images"><%= user.fullname %></a></li>
          <li><a href="/logout">Logout</a></li>
       <%if (album && !systemf) {%><li><a href="#upload" data-toggle="modal" data-target="#upload">Upload</a><li><% } %>
      </ul>
    </div>
  </div>
</nav>